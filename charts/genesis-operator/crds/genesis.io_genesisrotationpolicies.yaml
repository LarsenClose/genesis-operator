---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: genesisrotationpolicies.genesis.io
spec:
  group: genesis.io
  names:
    kind: GenesisRotationPolicy
    listKind: GenesisRotationPolicyList
    plural: genesisrotationpolicies
    singular: genesisrotationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source.name
      name: Source
      type: string
    - jsonPath: .spec.schedule.interval
      name: Interval
      type: string
    - jsonPath: .status.lastRotation.time
      name: LastRotation
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GenesisRotationPolicy is the Schema for the genesisrotationpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GenesisRotationPolicySpec defines the desired state of GenesisRotationPolicy
            properties:
              notify:
                description: Notify defines notification channels for rotation events
                items:
                  description: NotificationSpec defines a notification channel
                  properties:
                    channel:
                      description: Channel is the target channel (for Slack)
                      type: string
                    type:
                      description: Type is the notification type
                      enum:
                      - Event
                      - Slack
                      - Webhook
                      - PagerDuty
                      type: string
                    webhookSecretRef:
                      description: WebhookSecretRef references a secret containing
                        the webhook URL
                      properties:
                        key:
                          description: Key is the key within the secret
                          type: string
                        name:
                          description: Name is the name of the secret
                          type: string
                        namespace:
                          description: Namespace is the namespace of the secret (defaults
                            to policy namespace)
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  required:
                  - type
                  type: object
                type: array
              schedule:
                description: Schedule defines when rotation should occur
                properties:
                  interval:
                    description: Interval is the duration between rotations (e.g.,
                      "720h" for 30 days)
                    pattern: ^[0-9]+(h|m|s)$
                    type: string
                required:
                - interval
                type: object
              source:
                description: Source specifies the secret to rotate
                properties:
                  kind:
                    default: Secret
                    description: Kind is the Kubernetes resource kind (typically "Secret")
                    type: string
                  name:
                    description: Name is the name of the resource
                    type: string
                  namespace:
                    description: Namespace is the namespace of the resource
                    type: string
                required:
                - name
                - namespace
                type: object
              strategy:
                description: Strategy defines how rotation should be performed
                properties:
                  overlapPeriod:
                    description: OverlapPeriod is how long old and new secrets coexist
                      (for BlueGreen)
                    type: string
                  type:
                    default: BlueGreen
                    description: Type is the rotation strategy type
                    enum:
                    - BlueGreen
                    - Rolling
                    - Immediate
                    type: string
                type: object
              suspend:
                description: Suspend allows temporarily disabling rotation
                type: boolean
            required:
            - schedule
            - source
            type: object
          status:
            description: GenesisRotationPolicyStatus defines the observed state of
              GenesisRotationPolicy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastRotation:
                description: LastRotation contains information about the last successful
                  rotation
                properties:
                  message:
                    description: Message provides additional details about the rotation
                    type: string
                  newVersion:
                    description: NewVersion is the version of the new secret
                    type: string
                  oldVersion:
                    description: OldVersion is the version of the old secret
                    type: string
                  success:
                    description: Success indicates whether the rotation was successful
                    type: boolean
                  time:
                    description: Time is when the rotation occurred
                    format: date-time
                    type: string
                required:
                - success
                - time
                type: object
              nextRotation:
                description: NextRotation is when the next rotation is scheduled
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              rotationCount:
                description: RotationCount is the total number of successful rotations
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
